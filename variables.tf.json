// variables.tf.json

{
  "type": "object",
  "properties": {
    "organization_name": {
      "type": "string"
    },
    "workspace_configurations": {
      "type": "object",
      "additionalProperties": {
        "type": "object",
        "properties": {
          "auto_apply": {
            "type": "boolean"
          },
          "queue_all_runs": {
            "type": "boolean"
          }
        },
        "required": ["auto_apply", "queue_all_runs"]
      }
    },
    "azure_subscription_id": {
      "type": "string"
    },
    "azure_region": {
      "type": "string"
    }
  },
  "required": ["organization_name", "workspace_configurations", "azure_subscription_id", "azure_region"]
}
